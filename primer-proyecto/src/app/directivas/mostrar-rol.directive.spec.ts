import { MostrarRolDirective } from './mostrar-rol.directive';

describe('MostrarRolDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarRolDirective();
    expect(directive).toBeTruthy();
  });
});
